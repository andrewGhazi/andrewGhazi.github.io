# Open the anpan vignette

This function checks if you have the vignette built, then opens it with
utils::RShowDoc() if you do. This function exists just to make it a
little easier for users to find their way to the vignette.

## Usage

``` r
anpan_vignette()
```
